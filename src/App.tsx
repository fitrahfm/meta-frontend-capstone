import { useState } from 'react'
import { Layout } from './components/layout'

import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <Layout>
      Test
    </Layout>
  )
}

export default App
